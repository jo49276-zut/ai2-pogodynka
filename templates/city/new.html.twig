{% extends 'base.html.twig' %}

{% block title %}New City{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center">Create New City</h1>
        {{ include('city/_form.html.twig') }}
        <a href="{{ path('app_city_index') }}" class="btn btn-secondary mt-2">Back to list</a>
    </div>
{% endblock %}